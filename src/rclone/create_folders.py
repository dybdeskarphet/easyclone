# TODO: Create a create_folders function that creates the folders on the remote before backing up
# 1. Create a list from the uniquie directory paths.
# 2. Create a tree from the each directory (use basename for files, nothing is required for dirs)
# 2. Create directories using BFS concurrently.
